import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Login | Friends Space",
  description: "Login page of friends space",
};

function LoginPage() {
  return <div>LoginPage</div>;
}

export default LoginPage;
